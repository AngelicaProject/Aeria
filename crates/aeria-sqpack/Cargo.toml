[package]
name = "aeria-sqpack"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Read-only access to an installed game's SqPack archives and Excel sheets."

[dependencies]
crc32fast = "1.5.2"
flate2 = "1.1.10"
thiserror = "2.0.20"

[dev-dependencies]
rusqlite = { version = "0.40.2", features = ["bundled"] }

[lints]
workspace = true
